# 中文牙科 v2：挖矿扩充 + 重训

> 目标：验证「同域加数据」（更多 CMExam 口腔题）能否让 Qwen2.5-32B 学生超越教师（教师 86.90%）。
> 前情：交接文档结论是牙科场景最多打平；唯一没试过的杠杆就是「同域加数据」，此前以为没有，实际有近万道。

---

## 1. 数据结论（纠正此前的「只用了 train.csv」假设）

| 文件 | 行数 | 学科标签 | 状态 |
|---|---|---|---|
| `data/cmexam/train.csv` | 54497 | 无 | **此前从未使用** |
| `data/cmexam/val.csv` | 6811 | 无 | **此前从未使用** |
| `data/cmexam/test_with_annotations.csv` | 6811 | 有 | 已用（830 口腔医学，全部在旧 `cn_dental` 中） |

三文件两两不相交。旧管线只用了 test 切分（830 口腔医学 → 内部重切 train 580/val 125/test 125 → 内容重筛 381/80/84）。

---

## 2. 严格过滤（抬高 precision）

新增 `check_cn_dental_content.content_signal_strict`：
- 剔除单字歧义 `牙/齿/颌/龈/腭`（solo 不算，须命中多字牙科词；`龋` 保留）。
- 剔除泛化/神经/普外歧义词（三叉神经/面神经/海绵窦/切开引流/流涎/血管瘤/弹响/角化层/黏液腺 等）。
- 叠加英文 R1（不含 R2 oral 语境）。

在 test 地面真值上校准：

| 口径 | Precision | Recall |
|---|---|---|
| 全量 content_signal | 76.6% | 64.8% |
| **严格 content_signal_strict** | **87.3%** | 58.6% |

剩余 FP 绝大多数是「牙科内容但被打上解剖/药理/传染病/中药学标签」的题（口腔溃疡用药、硬腭组成、
颌骨含气骨、味蕾、牙龈肿痛药、流行性腮腺炎部位…），对口腔执业考试训练实际仍有用；真噪声极少。

---

## 3. 挖矿结果

`data/prepare_cn_dental_v2.py`（只挖 train/val 切分，不碰 test 切分，避免训练-测试分布泄漏）：

| 文件 | 总行 | 5选项单答案 | 严格牙科 |
|---|---|---|---|
| train.csv | 54497 | 51326 | **5681** |
| val.csv | 6811 | 6403 | **700** |

去重后（避开 test 84 / val 80 / 原 train 381，重叠 0）新增 **6381** 题。

**`data/cn_dental_clean/train_v2.jsonl` = 原 381 + 新 6381 = 6762 题**（答案分布均衡 A~E 各 17–22%）。

---

## 4. 重训结果

`run_retrain_cn_v2_queue.sh`：Qwen2.5-32B，配置与历史最优一致（rank16/lr2e-4/1epoch/α0），
3 seed（11/42/8）。val(80)/test(84) 保持不变。

| seed | 旧（381 题） | 新（6762 题） | 变化 |
|---|---|---|---|
| 11 | 88.10% (74/84) | 85.71% (72/84) | −2.39 |
| 42 | 88.10% (74/84) | **90.48% (76/84)** | +2.38 |
| 8  | 84.52% (71/84) | 84.52% (71/84) | 0.00 |
| **均值** | **86.90% (73/84)** | **86.90% (73/84)** | **0.00** |

- 教师 DeepSeek-V4-flash 零样本 = **86.90% (73/84)**。

## 4b. 超参扫描（6762 题，验证「打平」对调参稳健）

固定 seed=42、α=0、rank16/alpha32，网格 lr{1e-4,2e-4,3e-4}×epoch{1,2}，test 84：

| 配置 | val acc | test acc |
|---|---|---|
| **lr2e-4, 1 epoch（原配置）** | 88.75% | **90.48% (76/84)** |
| lr1e-4, 1 epoch | 92.50% | 89.29% (75/84) |
| lr3e-4, 1 epoch | 90.00% | 83.33% (70/84) |
| lr1e-4, 2 epochs | 90.00% | 88.1% (74/84) |
| lr2e-4, 2 epochs | 88.75% | 82.14% (69/84) |
| lr3e-4, 2 epochs | 86.25% | 82.14% (69/84) |

**结论：原配置（lr2e-4/1epoch）已是单 seed 最优，任何调参都超不过它，2 epoch 反而掉点（82–88%）。
「打平」不是选错了超参，而是选到了最优配置仍打平。**

## 5. 结论（「同域加数据」这一最后杠杆的实测）

**训练数据从 381 题扩到 6762 题（18 倍），3-seed 均值纹丝不动，仍是 86.90% = 教师，打平。**

- seed 42 单点冲到 90.48%（76/84，历史最高单点），但 seed 11 回落 85.71%，8 不变——纯属 84 题测试的 ±~5pp 噪声内抖动，均值无变化。
- **这坐实了最终结论：牙科「超越」的瓶颈不是训练数据量，而是教师的天花板。** 学生蒸馏增益恰好填平
  「教师零样本领先幅度」（headroom 7.14pp：79.76% → 86.90%），到教师水平即止，加再多同域数据也超不过。
- 至此，交接过里列的「调参 / 换学生 / 换蒸馏机制 / 跨域互译 / 多选拆分重组 / 放宽过滤回收 / 同域加数据」
  全部手段均已试尽，**牙科场景最多打平、无法超越**，最终结论定稿。

---

## 6. 关键文件

- 过滤：`data/check_cn_dental_content.py`（新增 `content_signal_strict` / `CN_SINGLE_CHARS` / `CN_BLOCKLIST`）
- 挖矿：`data/prepare_cn_dental_v2.py` → `data/cn_dental_clean/train_v2.jsonl`
- 校准/审计：`data/audit_cmexam_strict.py`、`data/audit_cmexam_unused.py`、`data/audit_cmexam_precision.py`
- 训练：`run_retrain_cn_v2_queue.sh`，日志 `retrain_cn_v2_queue.log`
- 产物：`experiments/cn_dental/runs/Qwen25_cn_v2_r16_lr2e4_s{11,42,8}`
